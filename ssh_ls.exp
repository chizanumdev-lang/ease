#!/usr/bin/expect -f
set timeout 20
set password "viicsoft"
set ip "173.212.248.253"

spawn ssh -o StrictHostKeyChecking=no root@$ip "ls -la"

expect {
    "*assword:*" {
        send "$password\r"
        exp_continue
    }
    eof
}
